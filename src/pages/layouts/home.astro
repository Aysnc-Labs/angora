---
import FullScreenLayout from '../../layouts/FullScreenLayout.astro';
import Card from '../../components/Card.astro';
---

<FullScreenLayout title="Home">
  <section class="@container">
    <div class="max-w-[var(--container-max)] mx-auto px-6 py-24">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold text-gray-900 tracking-tight text-wrap-balance">What's included</h2>
        <p class="text-lg text-gray-600 mt-4 max-w-[50ch] mx-auto text-wrap-pretty">Everything you need to launch faster, with none of the overhead you don't.</p>
      </div>
      <div class="grid grid-cols-1 @sm:grid-cols-2 @lg:grid-cols-3 gap-6">
        <Card variant="elevated">
          <div class="w-10 h-10 rounded-lg bg-primary-100 text-primary-600 flex items-center justify-center mb-4">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>
          </div>
          <p class="font-semibold text-gray-900 mb-2">Lightning fast</p>
          <p class="text-sm text-gray-600 text-wrap-pretty">Sub-100ms response times with edge computing and intelligent caching across 30+ regions.</p>
        </Card>
        <Card variant="elevated">
          <div class="w-10 h-10 rounded-lg bg-primary-100 text-primary-600 flex items-center justify-center mb-4">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
          </div>
          <p class="font-semibold text-gray-900 mb-2">Secure by default</p>
          <p class="text-sm text-gray-600 text-wrap-pretty">End-to-end encryption, SOC 2 compliance, and automatic key rotation out of the box.</p>
        </Card>
        <Card variant="elevated">
          <div class="w-10 h-10 rounded-lg bg-primary-100 text-primary-600 flex items-center justify-center mb-4">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
          </div>
          <p class="font-semibold text-gray-900 mb-2">Global scale</p>
          <p class="text-sm text-gray-600 text-wrap-pretty">Deploy to every continent with one click. Automatic failover and load balancing included.</p>
        </Card>
      </div>
    </div>
  </section>
</FullScreenLayout>
