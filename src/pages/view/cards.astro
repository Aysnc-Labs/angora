---
import FullScreenLayout from '../../layouts/FullScreenLayout.astro';
import Card from '../../components/Card.astro';
import ArrowRight from '../../components/icons/ArrowRight.astro';
---

<FullScreenLayout title="Cards">
  <div class="p-12 max-w-[var(--container-max)] mx-auto flex flex-col gap-12">

    <!-- Three variants side by side -->
    <div class="grid grid-cols-3 gap-6">
      <Card>
        <p class="font-semibold text-gray-900 mb-1">Default card</p>
        <p class="text-sm text-gray-600">Border and subtle shadow.</p>
      </Card>
      <Card variant="elevated">
        <p class="font-semibold text-gray-900 mb-1">Elevated card</p>
        <p class="text-sm text-gray-600">Shadow lifts on hover.</p>
      </Card>
      <Card variant="flat">
        <p class="font-semibold text-gray-900 mb-1">Flat card</p>
        <p class="text-sm text-gray-600">Tinted background only.</p>
      </Card>
    </div>

    <!-- Content cards with images -->
    <div class="grid grid-cols-3 gap-6">
      <Card padding={false}>
        <div class="h-40 bg-gray-200"></div>
        <div class="p-6">
          <p class="text-xs font-semibold text-primary-500 uppercase tracking-wide mb-2">Engineering</p>
          <p class="font-semibold text-gray-900 mb-2">Building reliable distributed systems</p>
          <p class="text-sm text-gray-600 mb-4">How we scaled our infrastructure to handle 10x traffic growth.</p>
          <a class="text-sm font-semibold text-primary-500 inline-flex items-center gap-1 hover:text-primary-600 transition-colors duration-fast ease-default" href="#">Read more <ArrowRight size={14} /></a>
        </div>
      </Card>
      <Card padding={false}>
        <div class="h-40 bg-gray-200"></div>
        <div class="p-6">
          <p class="text-xs font-semibold text-primary-500 uppercase tracking-wide mb-2">Product</p>
          <p class="font-semibold text-gray-900 mb-2">Announcing our new dashboard</p>
          <p class="text-sm text-gray-600 mb-4">A redesigned analytics experience with real-time insights.</p>
          <a class="text-sm font-semibold text-primary-500 inline-flex items-center gap-1 hover:text-primary-600 transition-colors duration-fast ease-default" href="#">Read more <ArrowRight size={14} /></a>
        </div>
      </Card>
      <Card padding={false}>
        <div class="h-40 bg-gray-200"></div>
        <div class="p-6">
          <p class="text-xs font-semibold text-primary-500 uppercase tracking-wide mb-2">Design</p>
          <p class="font-semibold text-gray-900 mb-2">Our approach to accessible color</p>
          <p class="text-sm text-gray-600 mb-4">Why we rebuilt our palette around WCAG AA from day one.</p>
          <a class="text-sm font-semibold text-primary-500 inline-flex items-center gap-1 hover:text-primary-600 transition-colors duration-fast ease-default" href="#">Read more <ArrowRight size={14} /></a>
        </div>
      </Card>
    </div>

    <!-- Feature cards -->
    <div class="grid grid-cols-3 gap-6">
      <Card variant="flat">
        <div class="w-10 h-10 rounded-lg bg-primary-100 text-primary-600 flex items-center justify-center mb-4">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>
        </div>
        <p class="font-semibold text-gray-900 mb-1">Lightning fast</p>
        <p class="text-sm text-gray-600">Sub-100ms response times with edge computing.</p>
      </Card>
      <Card variant="flat">
        <div class="w-10 h-10 rounded-lg bg-primary-100 text-primary-600 flex items-center justify-center mb-4">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
        </div>
        <p class="font-semibold text-gray-900 mb-1">Secure by default</p>
        <p class="text-sm text-gray-600">End-to-end encryption and SOC 2 compliance.</p>
      </Card>
      <Card variant="flat">
        <div class="w-10 h-10 rounded-lg bg-primary-100 text-primary-600 flex items-center justify-center mb-4">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
        </div>
        <p class="font-semibold text-gray-900 mb-1">Global scale</p>
        <p class="text-sm text-gray-600">Deploy to 30+ regions with one click.</p>
      </Card>
    </div>

    <!-- Testimonial -->
    <div class="max-w-lg">
      <Card variant="flat">
        <div class="flex gap-4">
          <div class="w-10 h-10 rounded-full bg-gray-300 shrink-0"></div>
          <div>
            <p class="text-sm text-gray-900 mb-2">"This completely changed how we think about our marketing pipeline. The analytics alone paid for itself in the first month."</p>
            <p class="text-xs font-semibold text-gray-900">Sarah Chen</p>
            <p class="text-xs text-gray-400">VP Marketing, Acme Corp</p>
          </div>
        </div>
      </Card>
    </div>

  </div>
</FullScreenLayout>
