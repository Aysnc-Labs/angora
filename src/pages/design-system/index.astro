---
import Layout from './_layout/Layout.astro';
---

<Layout title="Style Guide" description="Visual reference for all design tokens. Colors, type scale, spacing, shadows, and radii — the constraints that every component is built from.">

  <!-- Colors — Grey -->
  <section class="specimen-section">
    <h2>Grey Palette</h2>
    <p class="text-sm text-muted-foreground mb-6 max-w-[65ch]">
      Cool greys in OKLCH with <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">hue ~264°</code>. Chroma increases at the extremes to maintain warmth. Used for text hierarchy, borders, backgrounds, and structural elements.
    </p>
    <div class="border border-border rounded-lg overflow-hidden">
      <div class="flex">
        {[
          { shade: '50',  color: 'var(--gray-50)' },
          { shade: '100', color: 'var(--gray-100)' },
          { shade: '200', color: 'var(--gray-200)' },
          { shade: '300', color: 'var(--gray-300)' },
          { shade: '400', color: 'var(--gray-400)' },
          { shade: '500', color: 'var(--gray-500)' },
          { shade: '600', color: 'var(--gray-600)' },
          { shade: '700', color: 'var(--gray-700)' },
          { shade: '800', color: 'var(--gray-800)' },
          { shade: '900', color: 'var(--gray-900)' },
          { shade: '950', color: 'var(--gray-950)' },
        ].map(({ shade, color }) => (
          <div class="flex-1 flex flex-col">
            <div class="h-16" style={`background-color: ${color}`}></div>
            <div class="px-2 py-2 text-center">
              <span class="text-xs font-mono text-muted-foreground block">{shade}</span>
            </div>
          </div>
        ))}
      </div>
      <div class="border-t border-border px-6 py-4">
        <div class="flex gap-8 text-sm text-muted-foreground">
          <div><span class="font-semibold text-foreground">Text:</span> 900, 600, 400</div>
          <div><span class="font-semibold text-foreground">Borders:</span> 200, 300</div>
          <div><span class="font-semibold text-foreground">Backgrounds:</span> 50, 100</div>
          <div><span class="font-semibold text-foreground">Hue:</span> 264° (cool)</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Colors — Primary -->
  <section class="specimen-section">
    <h2>Primary Palette</h2>
    <p class="text-sm text-muted-foreground mb-6 max-w-[65ch]">
      Blue in OKLCH at <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">hue ~262°</code>, <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">chroma ~0.14</code>. 500 is the base — works as a button background with white text. 50–100 for tinted backgrounds, 600–700 for hover/active.
    </p>
    <div class="border border-border rounded-lg overflow-hidden">
      <div class="flex">
        {[
          { shade: '50',  color: 'var(--primary-50)' },
          { shade: '100', color: 'var(--primary-100)' },
          { shade: '200', color: 'var(--primary-200)' },
          { shade: '300', color: 'var(--primary-300)' },
          { shade: '400', color: 'var(--primary-400)' },
          { shade: '500', color: 'var(--primary-500)' },
          { shade: '600', color: 'var(--primary-600)' },
          { shade: '700', color: 'var(--primary-700)' },
          { shade: '800', color: 'var(--primary-800)' },
          { shade: '900', color: 'var(--primary-900)' },
          { shade: '950', color: 'var(--primary-950)' },
        ].map(({ shade, color }) => (
          <div class="flex-1 flex flex-col">
            <div class="h-16" style={`background-color: ${color}`}></div>
            <div class="px-2 py-2 text-center">
              <span class="text-xs font-mono text-muted-foreground block">{shade}</span>
            </div>
          </div>
        ))}
      </div>
      <div class="border-t border-border px-6 py-4">
        <div class="flex gap-8 text-sm text-muted-foreground">
          <div><span class="font-semibold text-foreground">Button bg:</span> 500</div>
          <div><span class="font-semibold text-foreground">Hover:</span> 600</div>
          <div><span class="font-semibold text-foreground">Active:</span> 700</div>
          <div><span class="font-semibold text-foreground">Focus ring:</span> 300</div>
          <div><span class="font-semibold text-foreground">Tint bg:</span> 50</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Type Scale -->
  <section class="specimen-section">
    <h2>Type Scale</h2>
    <p class="text-sm text-muted-foreground mb-6 max-w-[65ch]">
      Hand-crafted scale, not a modular ratio. Each step has a paired line-height — tighter for display sizes, more generous for body text. Font: <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">system-ui</code> stack.
    </p>
    <div class="border border-border rounded-lg overflow-hidden">
      {[
        { name: 'text-xs',  size: '12px', lh: '1.75', sample: 'Caption and fine print' },
        { name: 'text-sm',  size: '14px', lh: '1.5',  sample: 'Labels, hints, and secondary text' },
        { name: 'text-base', size: '16px', lh: '1.5', sample: 'Body text — the default reading size' },
        { name: 'text-lg',  size: '18px', lh: '1.5',  sample: 'Large body or intro paragraph' },
        { name: 'text-xl',  size: '20px', lh: '1.5',  sample: 'Section subheading' },
        { name: 'text-2xl', size: '24px', lh: '1.2',  sample: 'Section heading' },
        { name: 'text-3xl', size: '30px', lh: '1.2',  sample: 'Page heading' },
        { name: 'text-4xl', size: '36px', lh: '1.2',  sample: 'Large heading' },
        { name: 'text-5xl', size: '48px', lh: '1.0',  sample: 'Display' },
        { name: 'text-6xl', size: '60px', lh: '1.0',  sample: 'Hero' },
        { name: 'text-7xl', size: '72px', lh: '1.0',  sample: 'Hero XL' },
      ].map(({ name, size, lh, sample }, i, arr) => (
        <div class={`flex items-baseline gap-6 px-6 py-4 ${i < arr.length - 1 ? 'border-b border-border' : ''}`}>
          <div class="w-24 shrink-0">
            <span class="text-xs font-mono text-muted-foreground">{name}</span>
          </div>
          <div class="w-20 shrink-0">
            <span class="text-xs font-mono text-muted-foreground">{size} / {lh}</span>
          </div>
          <p class={`${name} text-foreground truncate`}>{sample}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- Text Hierarchy -->
  <section class="specimen-section">
    <h2>Text Hierarchy</h2>
    <p class="text-sm text-muted-foreground mb-6 max-w-[65ch]">
      Three-tier color system for text. Dark for primary content, medium for secondary, light for tertiary. Only two weights: normal (<code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">400</code>) and semibold (<code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">600</code>).
    </p>
    <div class="border border-border rounded-lg p-8">
      <div class="max-w-lg flex flex-col gap-6">
        <div class="flex gap-4 items-baseline">
          <span class="w-20 shrink-0 text-xs font-mono text-muted-foreground">primary</span>
          <p class="text-base font-semibold text-foreground">This is primary text — headings, key content, emphasis.</p>
        </div>
        <div class="flex gap-4 items-baseline">
          <span class="w-20 shrink-0 text-xs font-mono text-muted-foreground">secondary</span>
          <p class="text-base text-muted-foreground">This is secondary text — descriptions, supporting content.</p>
        </div>
        <div class="flex gap-4 items-baseline">
          <span class="w-20 shrink-0 text-xs font-mono text-muted-foreground">tertiary</span>
          <p class="text-base text-muted-foreground">This is tertiary text — timestamps, captions, fine print.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Typography -->
  <section class="specimen-section">
    <h2>Typography</h2>
    <p class="text-sm text-muted-foreground mb-6 max-w-[65ch]">
      Composed prose specimen using the <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">prose</code> utility class. Shows headings, body text, lists, blockquotes, figures, and inline treatments in natural flow — evaluate the rhythm and spacing between elements, not just individual styles. Select any text to see <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">::selection</code> styling.
    </p>
    <div class="border border-border rounded-lg p-8 lg:p-12">
      <div class="prose max-w-[65ch]">

        <h1>The Foundation of Visual Systems</h1>
        <p>A design system is more than a collection of components — it's a <strong>shared language</strong> that bridges the gap between design intent and engineering execution. Every decision, from the weight of a heading to the rhythm between sections, serves the reader.</p>

        <h2>Hierarchy and Rhythm</h2>
        <p>Visual hierarchy guides the eye through content. Headings establish structure, body text carries the narrative, and <mark>emphasis marks the pivotal moments</mark> a reader shouldn't miss.</p>
        <p>The space between elements matters as much as the elements themselves. A heading couples tightly to the paragraph it introduces — but separates clearly from the content above it. This <em>asymmetric spacing</em> creates the rhythm that makes content feel considered rather than mechanical.</p>

        <h3>Inline Treatments</h3>
        <p>Text within a paragraph can carry <strong>strong emphasis</strong>, <em>subtle stress</em>, or <mark>highlighted importance</mark>. Use <code>inline code</code> for technical references, <kbd>⌘K</kbd> for keyboard shortcuts, and <del>strikethrough</del> for outdated information. Links like <a href="#">design token documentation</a> use the primary color with an underline offset. Superscripts handle footnotes<sup>1</sup> and ordinals like 2<sup>nd</sup>.</p>

        <h3>Lists</h3>
        <p>Unordered lists group related items without implying sequence:</p>
        <ul>
          <li>Tokens encode color, spacing, and typography decisions</li>
          <li>Components consume tokens — never hard-coded values</li>
          <li>Specimens render the component in every supported state</li>
        </ul>
        <p>Ordered lists communicate steps or priority:</p>
        <ol>
          <li>Define the token in the theme</li>
          <li>Apply it via utility classes or custom properties</li>
          <li>Verify the specimen renders correctly</li>
        </ol>

        <blockquote>
          <p>Typography exists to honor content.</p>
          <footer>— Robert Bringhurst, <cite>The Elements of Typographic Style</cite></footer>
        </blockquote>

        <h2>Image and Caption</h2>
        <p>Figures pair an image with a descriptive caption, providing context that the image alone cannot convey.</p>
        <figure>
          <div class="aspect-[3/2] bg-muted rounded-lg"></div>
          <figcaption>Fig. 1 — A placeholder demonstrating how figures and captions integrate into the typographic flow.</figcaption>
        </figure>

        <hr>

        <h4>Spacing Outside Prose</h4>
        <p>On cards, heroes, and other components, spacing is intentional and explicit rather than rhythm-based:</p>
        <ul>
          <li><strong>Tight grouping</strong> (heading + supporting text): <code>gap-2</code> (8px)</li>
          <li><strong>Standard grouping</strong> (heading + body + CTA): <code>gap-4</code> (16px)</li>
          <li><strong>Section rhythm</strong> (between content blocks): <code>gap-8</code> to <code>gap-12</code></li>
        </ul>

        <h5>Level Five Heading</h5>
        <p>Fifth-level headings are rare in practice but available when deep nesting is required — typically inside documentation or structured reference material.</p>

        <h6>Level Six Heading</h6>
        <p>The smallest heading level. Visually identical to semibold body text — distinguished only by its semantic role in the document outline.</p>

      </div>
      <!-- Prose rhythm reference -->
      <div class="border-t border-border mt-8 pt-4">
        <table class="w-full text-sm">
          <thead>
            <tr>
              <th class="text-left py-1.5 text-xs font-semibold text-muted-foreground uppercase tracking-wide">Transition</th>
              <th class="text-left py-1.5 text-xs font-semibold text-muted-foreground uppercase tracking-wide">Spacing</th>
            </tr>
          </thead>
          <tbody class="font-mono text-xs text-muted-foreground">
            <tr><td class="py-1.5">paragraph + paragraph</td><td class="py-1.5">1.25em (~20px)</td></tr>
            <tr><td class="py-1.5">* → h1 / h2 / h3</td><td class="py-1.5">1.4em (~22px)</td></tr>
            <tr><td class="py-1.5">* → h4 / h5 / h6</td><td class="py-1.5">1.2em (~19px)</td></tr>
            <tr><td class="py-1.5">heading → content</td><td class="py-1.5">0.4em (~6px)</td></tr>
            <tr><td class="py-1.5">li + li</td><td class="py-1.5">0.375em (~6px)</td></tr>
            <tr><td class="py-1.5">around hr</td><td class="py-1.5">2em (~32px)</td></tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- Spacing Scale -->
  <section class="specimen-section">
    <h2>Spacing Scale</h2>
    <p class="text-sm text-muted-foreground mb-6 max-w-[65ch]">
      Base 8px with ~25% jumps. Use for padding, margins, and gaps. Smaller values (4–16px) for tight grouping, medium (24–48px) for component spacing, large (64–128px) for section rhythm.
    </p>
    <div class="border border-border rounded-lg p-6">
      <div class="flex flex-col gap-3">
        {[
          { name: '1',  value: '4px',   w: 'w-1' },
          { name: '2',  value: '8px',   w: 'w-2' },
          { name: '3',  value: '12px',  w: 'w-3' },
          { name: '4',  value: '16px',  w: 'w-4' },
          { name: '6',  value: '24px',  w: 'w-6' },
          { name: '8',  value: '32px',  w: 'w-8' },
          { name: '12', value: '48px',  w: 'w-12' },
          { name: '16', value: '64px',  w: 'w-16' },
          { name: '20', value: '80px',  w: 'w-20' },
          { name: '24', value: '96px',  w: 'w-24' },
          { name: '32', value: '128px', w: 'w-32' },
        ].map(({ name, value, w }) => (
          <div class="flex items-center gap-4">
            <span class="text-xs font-mono text-muted-foreground w-8 shrink-0 text-right">{name}</span>
            <div class={`h-3 ${w} bg-primary rounded-sm`}></div>
            <span class="text-xs font-mono text-muted-foreground">{value}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Shadows -->
  <section class="specimen-section">
    <h2>Shadows</h2>
    <p class="text-sm text-muted-foreground mb-6 max-w-[65ch]">
      Four-level elevation scale using two-part shadows (direct light + ambient occlusion). Light source from above. Use <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">sm</code> for buttons, <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">md</code> for cards, <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">lg</code> for dropdowns, <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">xl</code> for modals.
    </p>
    <div class="border border-border rounded-lg p-8">
      <div class="flex flex-wrap gap-8 items-end">
        {[
          { name: 'shadow-sm', class: 'shadow-sm' },
          { name: 'shadow-md', class: 'shadow-md' },
          { name: 'shadow-lg', class: 'shadow-lg' },
          { name: 'shadow-xl', class: 'shadow-xl' },
        ].map(({ name, class: shadow }) => (
          <div class="flex flex-col items-center gap-3">
            <div class={`w-24 h-24 bg-card rounded-lg ${shadow}`}></div>
            <span class="text-xs font-mono text-muted-foreground">{name}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Border Radius -->
  <section class="specimen-section">
    <h2>Border Radius</h2>
    <p class="text-sm text-muted-foreground mb-6 max-w-[65ch]">
      Four values. <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">sm</code> for small buttons and badges, <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">md</code> for inputs and default buttons, <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">lg</code> for cards and containers, <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">full</code> for pills and avatars.
    </p>
    <div class="border border-border rounded-lg p-8">
      <div class="flex flex-wrap gap-8 items-end">
        {[
          { name: 'rounded-sm', value: '4px',    class: 'rounded-sm' },
          { name: 'rounded-md', value: '8px',    class: 'rounded-md' },
          { name: 'rounded-lg', value: '12px',   class: 'rounded-lg' },
          { name: 'rounded-full', value: '9999px', class: 'rounded-full' },
        ].map(({ name, value, class: radius }) => (
          <div class="flex flex-col items-center gap-3">
            <div class={`w-20 h-20 border-2 ${radius}`} style="background-color: var(--primary-100); border-color: var(--primary-300)"></div>
            <span class="text-xs font-mono text-muted-foreground">{name}</span>
            <span class="text-xs font-mono text-muted">{value}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Transitions -->
  <section class="specimen-section">
    <h2>Transitions</h2>
    <p class="text-sm text-muted-foreground mb-6 max-w-[65ch]">
      Three duration tiers with a shared easing curve. <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">fast</code> for hover/focus micro-interactions, <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">normal</code> for state changes, <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">slow</code> for entrances and larger motions.
    </p>
    <div class="border border-border rounded-lg overflow-hidden">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-border bg-muted">
            <th class="text-left py-3 px-6 text-xs font-semibold text-muted-foreground uppercase tracking-wide">Token</th>
            <th class="text-left py-3 px-6 text-xs font-semibold text-muted-foreground uppercase tracking-wide">Value</th>
            <th class="text-left py-3 px-6 text-xs font-semibold text-muted-foreground uppercase tracking-wide">Use case</th>
          </tr>
        </thead>
        <tbody>
          <tr class="border-b border-border">
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">duration-fast</td>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">150ms</td>
            <td class="py-3 px-6 text-muted-foreground">Button hover/active, focus rings, color changes</td>
          </tr>
          <tr class="border-b border-border">
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">duration-normal</td>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">250ms</td>
            <td class="py-3 px-6 text-muted-foreground">Toggle switch, accordion expand, tab switch</td>
          </tr>
          <tr class="border-b border-border">
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">duration-slow</td>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">400ms</td>
            <td class="py-3 px-6 text-muted-foreground">Modal entrance, page transitions, skeleton loading</td>
          </tr>
          <tr>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">ease-default</td>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">cubic-bezier(0.4, 0, 0.2, 1)</td>
            <td class="py-3 px-6 text-muted-foreground">All transitions — fast start, gentle deceleration</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

  <!-- Layout -->
  <section class="specimen-section">
    <h2>Layout</h2>
    <p class="text-sm text-muted-foreground mb-6 max-w-[65ch]">
      Content constrained to <code class="text-xs bg-muted px-1.5 py-0.5 rounded-sm font-mono">--container-max: 1200px</code>. Container queries for responsive behavior — no viewport media queries in components.
    </p>
    <div class="border border-border rounded-lg overflow-hidden">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-border bg-muted">
            <th class="text-left py-3 px-6 text-xs font-semibold text-muted-foreground uppercase tracking-wide">Token</th>
            <th class="text-left py-3 px-6 text-xs font-semibold text-muted-foreground uppercase tracking-wide">Value</th>
            <th class="text-left py-3 px-6 text-xs font-semibold text-muted-foreground uppercase tracking-wide">Notes</th>
          </tr>
        </thead>
        <tbody>
          <tr class="border-b border-border">
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">container-max</td>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">1200px</td>
            <td class="py-3 px-6 text-muted-foreground">Max content width, centered with mx-auto</td>
          </tr>
          <tr class="border-b border-border">
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">@sm</td>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">640px</td>
            <td class="py-3 px-6 text-muted-foreground">Container query breakpoint</td>
          </tr>
          <tr class="border-b border-border">
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">@md</td>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">768px</td>
            <td class="py-3 px-6 text-muted-foreground">Container query breakpoint</td>
          </tr>
          <tr class="border-b border-border">
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">@lg</td>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">1024px</td>
            <td class="py-3 px-6 text-muted-foreground">Container query breakpoint</td>
          </tr>
          <tr>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">@xl</td>
            <td class="py-3 px-6 font-mono text-xs text-muted-foreground">1280px</td>
            <td class="py-3 px-6 text-muted-foreground">Container query breakpoint</td>
          </tr>
        </tbody>
      </table>
    </div>
  </section>

</Layout>
