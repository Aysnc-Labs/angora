---
import FullScreen from '../_layout/FullScreen.astro';
import Section from '@/components/Section.astro';
import Card from '@/components/Card/Card.astro';
import CardBody from '@/components/Card/CardBody.astro';
import CardEyebrow from '@/components/Card/CardEyebrow.astro';
import CardTitle from '@/components/Card/CardTitle.astro';
import CardDescription from '@/components/Card/CardDescription.astro';
import ArrowRight from '@/icons/ArrowRight.astro';
---

<FullScreen title="Home Layout">

  <!-- Hero -->
  <Section seamless class="bg-muted">
    <div class="text-center py-16">
      <h1 class="text-5xl font-bold tracking-tight text-foreground mb-4">Ship faster with better tools</h1>
      <p class="text-lg text-muted-foreground max-w-[50ch] mx-auto">Everything your team needs to build, deploy, and scale — without the infrastructure headaches.</p>
    </div>
  </Section>

  <!-- Features -->
  <Section withPadding>
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold tracking-tight text-foreground mb-2">Built for modern teams</h2>
      <p class="text-base text-muted-foreground max-w-[50ch] mx-auto">Three pillars that make the difference.</p>
    </div>
    <div class="grid grid-cols-1 @sm:grid-cols-2 @lg:grid-cols-3 grid-gap">
      <Card variant="flat">
        <div class="w-10 h-10 rounded-lg bg-primary/10 text-primary flex items-center justify-center mb-4">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>
        </div>
        <CardTitle>Lightning fast</CardTitle>
        <CardDescription>Sub-100ms response times with edge computing and intelligent caching.</CardDescription>
      </Card>
      <Card variant="flat">
        <div class="w-10 h-10 rounded-lg bg-primary/10 text-primary flex items-center justify-center mb-4">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
        </div>
        <CardTitle>Secure by default</CardTitle>
        <CardDescription>End-to-end encryption, SOC 2 compliance, and automatic key rotation.</CardDescription>
      </Card>
      <Card variant="flat">
        <div class="w-10 h-10 rounded-lg bg-primary/10 text-primary flex items-center justify-center mb-4">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
        </div>
        <CardTitle>Global scale</CardTitle>
        <CardDescription>Deploy to 30+ regions with one click. Automatic failover and load balancing.</CardDescription>
      </Card>
    </div>
  </Section>

  <!-- Blog / Content Cards -->
  <Section seamless class="bg-muted">
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold tracking-tight text-foreground mb-2">From the blog</h2>
      <p class="text-base text-muted-foreground max-w-[50ch] mx-auto">Latest thinking from the team.</p>
    </div>
    <div class="grid grid-cols-1 @sm:grid-cols-2 @lg:grid-cols-3 grid-gap">
      <Card>
        <CardEyebrow>Engineering</CardEyebrow>
        <CardTitle>Building reliable distributed systems</CardTitle>
        <CardDescription>How we scaled our infrastructure to handle 10x traffic growth.</CardDescription>
        <a class="text-sm font-semibold text-primary inline-flex items-center gap-1 hover:text-primary-hover transition-colors duration-fast ease-default mt-4" href="#">Read more <ArrowRight size={14} /></a>
      </Card>
      <Card>
        <CardEyebrow>Product</CardEyebrow>
        <CardTitle>Announcing our new dashboard</CardTitle>
        <CardDescription>A redesigned analytics experience with real-time insights.</CardDescription>
        <a class="text-sm font-semibold text-primary inline-flex items-center gap-1 hover:text-primary-hover transition-colors duration-fast ease-default mt-4" href="#">Read more <ArrowRight size={14} /></a>
      </Card>
      <Card>
        <CardEyebrow>Design</CardEyebrow>
        <CardTitle>Our approach to accessible color</CardTitle>
        <CardDescription>Why we rebuilt our palette around WCAG AA from day one.</CardDescription>
        <a class="text-sm font-semibold text-primary inline-flex items-center gap-1 hover:text-primary-hover transition-colors duration-fast ease-default mt-4" href="#">Read more <ArrowRight size={14} /></a>
      </Card>
    </div>
  </Section>

  <!-- Testimonial -->
  <Section withPadding>
    <div class="text-center mb-12">
      <h2 class="text-3xl font-bold tracking-tight text-foreground mb-2">What people are saying</h2>
    </div>
    <div class="grid grid-cols-1 @sm:grid-cols-2 grid-gap max-w-3xl mx-auto">
      <Card variant="flat">
        <div class="flex gap-4">
          <div class="w-10 h-10 rounded-full bg-input shrink-0"></div>
          <div>
            <p class="text-sm text-foreground mb-2">"This completely changed how we think about our marketing pipeline. The analytics alone paid for itself in the first month."</p>
            <p class="text-xs font-semibold text-foreground">Sarah Chen</p>
            <p class="text-xs text-muted-foreground">VP Marketing, Acme Corp</p>
          </div>
        </div>
      </Card>
      <Card variant="flat">
        <div class="flex gap-4">
          <div class="w-10 h-10 rounded-full bg-input shrink-0"></div>
          <div>
            <p class="text-sm text-foreground mb-2">"We cut our deploy time from 45 minutes to under 3. The team hasn't looked back since."</p>
            <p class="text-xs font-semibold text-foreground">Marcus Rivera</p>
            <p class="text-xs text-muted-foreground">CTO, Wavelength</p>
          </div>
        </div>
      </Card>
    </div>
  </Section>

</FullScreen>
