---
interface Props {
  variant?: 'primary' | 'secondary' | 'ghost';
  size?: 'sm' | 'md' | 'lg';
  disabled?: boolean;
}

const { variant = 'primary', size = 'md', disabled } = Astro.props;

const sizeClasses: Record<string, string> = {
  sm: 'text-sm px-4 py-2 rounded-sm',
  md: 'text-base px-6 py-3 rounded-md',
  lg: 'text-lg px-8 py-4 rounded-md',
};

const base = 'inline-flex items-center justify-center gap-2 font-semibold cursor-pointer';

function resolveClasses(): string {
  const parts = [base, sizeClasses[size]];

  if (disabled) {
    parts.push('opacity-50 cursor-not-allowed');
  }

  if (variant === 'primary') {
    parts.push(
      'bg-primary text-primary-foreground',
      'hover:bg-primary-hover',
      'active:bg-primary-active active:translate-y-px',
      'focus-visible:outline-2 focus-visible:outline-ring focus-visible:outline-offset-2',
      'transition-colors duration-fast ease-default',
    );
  } else if (variant === 'secondary') {
    parts.push(
      'bg-transparent text-foreground shadow-[inset_0_0_0_1px_var(--color-border)]',
      'hover:bg-accent',
      'active:bg-accent active:translate-y-px',
      'focus-visible:outline-2 focus-visible:outline-ring focus-visible:outline-offset-2',
      'transition-colors duration-fast ease-default',
    );
  } else if (variant === 'ghost') {
    parts.push(
      'bg-transparent text-muted-foreground',
      'hover:bg-accent hover:text-accent-foreground',
      'active:bg-accent active:translate-y-px',
      'focus-visible:outline-2 focus-visible:outline-ring focus-visible:outline-offset-2',
      'transition-colors duration-fast ease-default',
    );
  }

  return parts.join(' ');
}

const classes = resolveClasses();
---

<button
  data-component="Button"
  class={classes}
  disabled={disabled || undefined}
>
  <slot />
</button>
