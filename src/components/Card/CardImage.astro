---
interface Props {
  src?: string;
  alt?: string;
  height?: string;
}

const { src, alt = '', height = 'h-40' } = Astro.props;
---

{src ? (
  <img src={src} alt={alt} class={`w-full ${height} object-cover`} />
) : (
  <div class={`w-full ${height} bg-gray-200`}></div>
)}
