/**
 * Design system configuration.
 * Imported by Layout.astro, Sidebar.tsx, FullscreenLink.tsx,
 * and the view/[theme]/[...slug].astro route.
 */
export const config = {
  /** Enable dark mode toggle, dark view routes, and theme switching. */
  darkMode: true,
};
